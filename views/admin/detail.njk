{% extends "admin/layout.njk" %}

{% block head %}
    <title>Receitas - Foodfy</title>
{% endblock %}

{% block content %}
    <section>
        <div class=title-btn>
            <h1>Receita: {{ items.title }} </h1>
            <span>
                <a href="/admin/recipes/{{ recipeIndex }}/edit"><button>Editar receita</button></a>
            </span>
        </div>

        <div class="card recipe">
            <img src="{{ items.image }}" alt="{{ items.title }}">
            <h1>{{ items.title }}</h1>
            <p>por
                {{ items.author }}</p>
        </div>

        <div class="recipe_content">

            <h1>Ingredientes</h1>

            <ul class="ingredient_">
                {% for ingredient in items.ingredients %}
                    <li>
                        {{ ingredient }}
                    </li>
                {% endfor %}
            </ul>

            <h1>Modo de preparo</h1>

            <ul class="preparation_">
                {% for step in items.preparation %}
                    <li>
                        {{ step }}
                    </li>
                {% endfor %}
            </ul>

            <h1>Informações adicionais</h1>

            <p class="information_">
                {{ items.information }}
            </p>
        </div>
    </section>

{% endblock %}